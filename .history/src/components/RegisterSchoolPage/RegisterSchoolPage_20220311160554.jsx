import React from "react";

function RegisterSchoolPage({ display }) {
  return (
    <section
      className={`${display ? "block" : "hidden"} bg-black h-screen w-full`}
    >
      RegisterSchoolPage
    </section>
  );
}

export default RegisterSchoolPage;
