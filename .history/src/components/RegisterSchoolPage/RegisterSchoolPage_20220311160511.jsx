import React from "react";

function RegisterSchoolPage({ display }) {
  return (
    <section className={`${display ? "block" : "hidden"}`}>
      RegisterSchoolPage
    </section>
  );
}

export default RegisterSchoolPage;
