import React from "react";

function RegisterSchoolPage({ display }) {
  return (
    <section className={`${display ? "hidden" : "block"}`}>
      RegisterSchoolPage
    </section>
  );
}

export default RegisterSchoolPage;
